import * as React from 'react';

const GalleryLoader = (): React.ReactElement => {
  return (
    <div className="loader-container">
      <span className="loader-animation">Loading...</span>
    </div>
  );
};

export default GalleryLoader;
